---
- name: vars from prompt
  hosts: localhost
  vars_prompt:
    - name: username
      prompt: enter the username
      private: false
    - name: password
      prompt: enter the password
      private: true
  tasks:
    - name: print the username and pass
      ansible:builtin:debug:
        msg: username is:{{username}},password is {{password}}
